old:
01_ping.svg: latency comparsion between latency-sensitiv slice and default slice, while video-traffic in the network
01_retry.svg: retry comparsion between mission-critical slive and default slice, while video-traffic in the network
01_throughput.svg: bitrate comparsion between mission-critical slice and default slice, while video-traffic in the network 
newer:
002_latency.svg: latency slice vs default slice while video traffic on default slice 
002_retry.svg: mission-critical slice vs default slice while video traffic on default slice, measuring retrys 
002_throughput.svg: mission-critical slice vs default slice while video traffic on default slice, measuring throughput
